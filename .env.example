# Copy to .env. Needed only by POST /scout/ocr.
GEMINI_API_KEY=your-key-here

# Frontend (Vite). Empty = same origin. On Vercel set to the Render API URL.
# VITE_API_URL=https://spraysense.onrender.com
